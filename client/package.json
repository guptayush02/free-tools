{
  "name": "ats-resume-optimizer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf ../server/public/* && cp -r dist/* ../server/public/",
    "build:server": "npm run build && rm -rf ../server/public/* && cp -r dist/* ../server/public/"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.7.0",
    "axios": "^1.3.4",
    "html2pdf.js": "^0.14.0",
    "monaco-editor": "^0.55.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.2.0"
  }
}
